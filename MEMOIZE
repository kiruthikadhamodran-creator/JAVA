var memoize = function(fn) {
    const cache = {};
    let callCount = 0;

    return function(...args) {
        const key = JSON.stringify(args);

        if (key in cache) {
            return cache[key];
        }

        callCount++;
        const result = fn(...args);
        cache[key] = result;
        return result;
    };
};
